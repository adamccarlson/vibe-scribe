#!/usr/bin/env node

// Vibe Scribe — session-start hook. STUB.
//
// Intent: on session start, support the "earned" briefing trigger — if a briefing is warranted
// (high change-volume since last session, a stale overview section, or the last session ended
// mid-task), surface a gentle nudge so the user can run /briefing to re-orient. Otherwise stay
// silent. Heuristics are an open design question (see PROTOCOL.md → Open items).
//
// No-op placeholder for now so the plugin loads cleanly.

process.exit(0);
